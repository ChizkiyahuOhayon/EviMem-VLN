[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "evimem-vln"
version = "0.1.0"
description = "Future- and dependence-aware evidence admission for continuous VLN"
readme = "README.md"
requires-python = ">=3.9"
license = {file = "LICENSE"}
authors = [{name = "EviMem-VLN contributors"}]
dependencies = ["numpy>=1.26,<3"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.optional-dependencies]
download = [
  "gdown==5.2.0",
  "huggingface-hub==0.23.2",
]
dev = ["pytest>=8,<9"]

[project.scripts]
evimem = "evimem.cli:main"
gavln-eval = "gavln.gavln_eval:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["evimem*", "gavln*", "llava*", "trl*", "vggt*"]
namespaces = true

[tool.setuptools.package-data]
evimem = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
